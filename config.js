// Global configuration attached to window to avoid module issues
window.CONFIG = {
    // Appearance
    colors: {
        bg: '#0f1724',
        panel: '#0b1220',
        accent: '#10b981',
        grid: '#15303a',
        snake: '#34d399',
        snakeHead: '#facc15',
        food: '#fb7185',
        text: '#e6eef6'
    },

    // Grid settings
    cols: 20,
    rows: 20,

    // Game Loop
    baseSpeed: 8, // Ticks per second for Free Mode / Level 1
};
